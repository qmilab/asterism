// @qmilab/asterism-reflect — default ReflectionProvider.
// Takes a run transcript, calls a hosted model, returns proposed typed memory
// writes with confidence. Pure TypeScript — no Python, no local ML, no
// embeddings in Phase 0. Only package permitted to import a reflection model
// client. No logic yet.

export {};
