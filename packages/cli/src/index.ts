// @qmilab/asterism — the `asterism` CLI.
// Thin surface: argument parsing, kernel calls, formatting. Wires the concrete
// RuntimeAdapter (adapter-pi) and ReflectionProvider (reflect) into the kernel
// at startup. No business logic. No logic yet.

export {};
