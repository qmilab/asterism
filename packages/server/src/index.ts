// @qmilab/asterism-server — thin local HTTP endpoint over the kernel.
// Phase 0 endpoints: POST /agents/:agent/runs, GET /agents/:agent/runs,
// GET /agents/:agent/events. No business logic — it calls the kernel.
// No logic yet.

export {};
